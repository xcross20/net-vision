# UI convergence

Approved reference images in `apps/web/public/brand/references/` are **composition and hierarchy** authority.

They are not domain-data authority. Do not copy invented counts, prices, Gear SKUs, discounts, Gacha nav, or premature payment availability.

Canonical Button Presser supply remains Plate `officialExistingSupply` (62093).

Primary nav is `PRIMARY_NAV` in `apps/web/lib/nav.ts`: Market, Categories, Activity, Portfolio. Gacha is out of scope.

Brand mark: `NetVisionMark` / `NetVisionLogo`. Decorative brand media must never replace canonical NFT `tokenURI` media.
