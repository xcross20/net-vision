{
  "name": "net-vision",
  "version": "0.1.0",
  "private": true,
  "description": "Net Vision: specialized marketplace and analytics terminal for Button Presser on Robinhood Chain.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "rm -rf apps/web/.next/server apps/web/.next/static apps/web/.next/standalone apps/web/.next/types && npm run build --workspace=apps/web",
    "start": "npm run start --workspace=apps/web",
    "start:worker": "npm run start --workspace=apps/market-worker",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.3",
    "happy-dom": "^15.11.7",
    "playwright": "^1.63.0"
  }
}
